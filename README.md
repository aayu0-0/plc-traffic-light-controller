# plc-traffic-light-controller
A traffic light controller in TIA Portal — built twice, once in ladder logic and once as an SCL function block, the second build specifically to prove how PLC scan-cycle read/write ordering actually behaves.
